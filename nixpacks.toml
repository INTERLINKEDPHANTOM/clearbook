[phases.setup]
nixPkgs = ["cairo", "pango", "gdk-pixbuf", "libffi", "fontconfig", "harfbuzz", "gobject-introspection", "freetype", "glib"]
[phases.install]
cmds = ["pip install -r requirements.txt"]
[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1 --no-access-log"
