[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "cairo", "pango", "gdk-pixbuf", "libffi", "fontconfig", "harfbuzz", "gobject-introspection", "freetype", "glib"]
[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1 --no-access-log"
